{
  "id": "generate_summary",
  "name": "Generate Summary",
  "description": "Generate a summary of the conversation for the patient to download",
  "type": "server_tool",
  "endpoint": "https://api.example.com/tools/generate-summary",
  "authentication": {
    "type": "api_key",
    "header": "X-API-Key"
  },
  "parameters": {
    "conversation_id": {
      "type": "string",
      "description": "The unique identifier of the conversation",
      "required": true
    },
    "report_id": {
      "type": "string",
      "description": "The unique identifier of the health report",
      "required": true
    },
    "format": {
      "type": "string",
      "enum": ["text", "pdf", "json"],
      "description": "Output format for the summary",
      "required": false,
      "default": "text"
    }
  },
  "response_schema": {
    "type": "object",
    "properties": {
      "summary_id": { "type": "string" },
      "content": { "type": "string" },
      "format": { "type": "string" },
      "created_at": { "type": "string" },
      "download_url": { "type": "string" }
    }
  },
  "created_at": "2024-12-11T21:08:00Z"
}
